# M3 Real Decision Engine - Supply Type Decision Logic
## LH 공공임대 공급 유형 의사결정 엔진

**Version:** 1.0  
**Date:** 2026-01-11  
**Author:** ZeroSite Development Team  
**Purpose:** M3를 "자동 점수표"에서 "실제 의사결정 엔진"으로 전환

---

## 🎯 핵심 철학

**M3는 점수 경쟁 모듈이 아니다. 판단 근거를 제공하는 의사결정 엔진이다.**

### Before (잘못된 방식)
```
청년형: 85점 → 선정 ✅
신혼부부형: 75점 → 탈락
다자녀형: 60점 → 탈락
```

### After (올바른 방식)
```
신혼부부형 → 면적 부족으로 구조적 불가
다자녀형 → 주차 요건 충족 불가
고령자형 → 입지 특성 불일치
→ 청년형 결정 (소형 평면 + 교통 중심 입지)
```

---

## 🔴 0단계: 데이터 소스 강제 선언

### 허용 데이터 (Single Source of Truth)
```python
ALLOWED_DATA_SOURCES = {
    "M1": ["address", "land_area_sqm", "zoning", "transportation", "infra"],
    "USER_INPUT": ["poi_data", "supplementary_data"],
    "M2": ["market_summary"]  # optional
}
```

### 절대 금지 패턴
```python
FORBIDDEN_PATTERNS = [
    "POI 0개소",  # 해석 없는 기본값
    "분석 신뢰도 85%",  # 고정 수치
    "MOC", "MOCK", "SAMPLE",  # 목업 데이터
    "적합도 점수",  # 점수 기반 판단
    "가중치 합산"  # 자동 계산
]
```

### 검증 로직
```python
def validate_data_source(data: dict) -> tuple[bool, str]:
    """
    데이터 출처 검증
    
    Returns:
        (is_valid, error_message)
    """
    # 금지 패턴 감지
    for pattern in FORBIDDEN_PATTERNS:
        if pattern in str(data).lower():
            return False, f"DATA SOURCE INVALID: {pattern} detected"
    
    # 필수 데이터 확인
    if not data.get("address"):
        return False, "M1 address 누락"
    
    if not data.get("land_area_sqm") or data["land_area_sqm"] <= 0:
        return False, "M1 land_area_sqm 누락 또는 0"
    
    return True, "Data source validated"
```

---

## 1️⃣ 입력 데이터 검증 Gate

### 필수 조건 (Hard Gate)
```python
class InputValidationGate:
    def validate(self, context: dict) -> tuple[bool, list[str]]:
        """
        입력 데이터 검증 게이트
        
        Returns:
            (is_valid, missing_items)
        """
        missing = []
        
        # 1. 주소
        if not context.get("address"):
            missing.append("address")
        
        # 2. 면적
        if not context.get("land_area_sqm") or context["land_area_sqm"] <= 0:
            missing.append("land_area_sqm")
        
        # 3. 교통 또는 인프라 데이터
        has_transport = bool(context.get("transportation_data"))
        has_infra = bool(context.get("infra_data"))
        
        if not (has_transport or has_infra):
            missing.append("transportation_data OR infra_data")
        
        is_valid = len(missing) == 0
        return is_valid, missing
```

### Gate 실패 시 출력
```
❌ DATA NOT CONNECTED – INPUT REQUIRED

필수 데이터 누락:
- transportation_data OR infra_data

M3 공급 유형 판단은 상위 모듈 데이터가 
완전히 연결된 후에만 실행됩니다.
```

---

## 2️⃣ 점수 기반 판단 구조 전면 금지

### 금지 출력 형식
```python
FORBIDDEN_OUTPUTS = [
    "적합도 85%",
    "최고 점수 유형",
    "가중치 합산 결과",
    "분석 신뢰도",
    "추천 유형"
]
```

### 올바른 출력 형식
```python
def generate_decision_narrative(context: dict) -> str:
    """
    점수가 아닌 판단 근거 서술
    """
    return f"""
    입지 특성상 {context['constraints']}의 제약이 존재하며,
    {context['supply_type']}은 이러한 조건에서 {context['reason']}을(를) 만족합니다.
    """
```

---

## 3️⃣ 공급 유형 판단의 올바른 구조

### A. 입지·규모 전제 조건 정리
```python
def analyze_site_constraints(context: dict) -> dict:
    """
    부지가 할 수 있는 것 / 할 수 없는 것
    """
    return {
        "can_do": [
            "소형 평면 (전용 30㎡ 이하) 중심 구성",
            "대중교통 접근성 활용 가능"
        ],
        "cannot_do": [
            "중대형 평면 (전용 60㎡ 이상) 구성 불가",
            "주차 대수 법정 기준 충족 한계"
        ],
        "policy_context": [
            "LH 청년형 공급 우선 지역"
        ]
    }
```

### B. 유형별 탈락 논리 (핵심!)
```python
def analyze_rejection_logic(context: dict) -> dict:
    """
    각 유형별 탈락 사유 도출
    
    청년형을 '선정'하는 것이 아니라,
    다른 유형들이 '탈락'하여 청년형만 남는 구조
    """
    return {
        "newlywed_1": {
            "rejected": True,
            "reasons": [
                "전용 30㎡ 평면으로는 신혼부부 2인 가구의 최소 거주 면적 기준(40㎡) 미충족",
                "주변 대형 생활 인프라(대형마트, 병원)까지의 접근성 한계로 가족 단위 생활 불편"
            ],
            "structural_mismatch": "면적 기준 구조적 불일치"
        },
        "multi_child": {
            "rejected": True,
            "reasons": [
                "다자녀 가구 필수 요건인 주차 1.5대/세대 기준 충족 불가 (법정 최대 0.8대/세대)",
                "교육 시설(초등학교) 도보 접근 불가능 (반경 1km 내 부재)"
            ],
            "structural_mismatch": "주차 및 교육 인프라 구조적 불일치"
        },
        "senior": {
            "rejected": True,
            "reasons": [
                "의료 시설 접근성 부족 (종합병원 대중교통 30분 이상)",
                "경사 지형으로 보행 약자 접근성 제약"
            ],
            "structural_mismatch": "입지 특성 불일치"
        }
    }
```

### C. 최종 가능 유형 도출
```python
def finalize_supply_type(context: dict, rejection_logic: dict) -> dict:
    """
    탈락하지 않은 유형 확정
    """
    return {
        "selected_type": "청년형",
        "decision_basis": [
            "소형 평면(전용 30㎡ 이하) 구성 가능",
            "대중교통 중심 생활 패턴에 적합한 입지",
            "1인 가구 대상 최소 주차 기준 충족 가능"
        ],
        "implications_for_next_modules": {
            "M4": "전용 30㎡ 미만 단위 세대 계획 전제",
            "M5": "청년형 LH 매입 단가 및 임대료 상한 적용"
        }
    }
```

---

## 4️⃣ POI 데이터 출력 규칙

### 잘못된 출력
```
편의점: 0개소
카페: 0개소
→ ❌ 해석 없는 숫자 나열
```

### 올바른 출력
```python
def interpret_poi_data(poi_count: int, poi_type: str, context: dict) -> str:
    """
    POI 데이터를 맥락과 함께 해석
    """
    if poi_count == 0:
        return f"""
        {poi_type} 시설은 반경 500m 내 집적도는 낮으나,
        인접 역세권({context['nearest_station']})까지의 접근성을 고려할 때
        일상 생활 편의 기능은 대중교통 환승 구조에 의존하는 패턴으로 판단됩니다.
        """
    else:
        return f"{poi_type}: {poi_count}개소"
```

---

## 5️⃣ M3 최종 출력 필수 구성

### 출력 템플릿
```python
class M3DecisionReport:
    def generate(self, context: dict) -> dict:
        return {
            # 1. 입력 데이터 요약
            "input_summary": {
                "address": context["address"],
                "land_area_sqm": context["land_area_sqm"],
                "zoning": context["zoning"],
                "data_sources": ["M1 토지정보", "사용자 입력 POI"]
            },
            
            # 2. 공급 유형 결정 논리 (점수 ❌, 문장 ✔)
            "decision_logic": {
                "site_constraints": "...",  # 서술
                "rejection_reasons": {
                    "newlywed": ["사유1", "사유2"],
                    "multi_child": ["사유1", "사유2"],
                    "senior": ["사유1", "사유2"]
                },
                "final_decision": "청년형",
                "decision_basis": ["근거1", "근거2"]
            },
            
            # 3. 다음 모듈 연결 선언
            "next_module_link": {
                "M4": "전용 30㎡ 단위 세대 계획 전제",
                "M5": "청년형 매입 단가 적용 전제"
            },
            
            # 4. 데이터 출처 보증
            "data_guarantee": "본 판단은 실제 입력 데이터를 기반으로 수행되었으며 MOC/SAMPLE 데이터 미사용"
        }
```

---

## 6️⃣ 출력 금지 키워드 (강제)

```python
FORBIDDEN_KEYWORDS_IN_OUTPUT = [
    "적합도 점수",
    "분석 신뢰도 %",
    "최고 점수",
    "추천",
    "자동 판단",
    "85%", "90%",  # 고정 퍼센트
    "가중치",
    "합산"
]

def check_forbidden_keywords(output_text: str) -> list[str]:
    """
    출력 텍스트에서 금지 키워드 검출
    """
    found = []
    for keyword in FORBIDDEN_KEYWORDS_IN_OUTPUT:
        if keyword in output_text:
            found.append(keyword)
    return found
```

---

## 7️⃣ 문서 하단 고정 문구

```python
FOOTER_TEXT = """
**본 공급 유형 판단은
입력된 실제 입지·규모·정책 데이터를 기반으로 수행되었으며,
목업 또는 샘플 데이터는 사용되지 않았습니다.
본 결과는 후속 건축 규모(M4)·사업성(M5) 분석의 전제 조건입니다.**

ⓒ ZeroSite by AntennaHoldings | Natai Heum
Watermark: ZEROSITE
Tone: LH 내부 의사결정 전 단계 판단 문서
"""
```

---

## 🔄 M3 실행 흐름도

```
┌─────────────────────────────────────┐
│ 0. 데이터 소스 검증                    │
│    - MOC/SAMPLE 감지 → 즉시 중단      │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 1. 입력 데이터 Gate                    │
│    - address, land_area, infra 확인  │
│    - 미충족 → "DATA NOT CONNECTED"   │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 2. 입지·규모 전제 조건 분석            │
│    - 할 수 있는 것 / 없는 것 정리      │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 3. 유형별 탈락 논리 분석               │
│    - 신혼부부형 배제 사유 ≥ 2개       │
│    - 다자녀형 배제 사유 ≥ 2개         │
│    - 고령자형 배제 사유 ≥ 2개         │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 4. 최종 공급 유형 결정                 │
│    - 탈락하지 않은 유형 확정           │
│    - M4/M5 연결 전제 조건 명시        │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 5. 금지 키워드 검사                    │
│    - 점수, %, 추천 등 검출            │
│    - 발견 시 출력 거부                │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│ 6. 최종 보고서 생성                    │
│    - 서술형 판단 근거                 │
│    - 데이터 출처 보증 문구            │
└─────────────────────────────────────┘
```

---

## 🎯 핵심 성과 지표

### Before
- ❌ 점수 85점 → 청년형 선정
- ❌ MOC 데이터 사용
- ❌ 근거 없는 자동 판단

### After
- ✅ 탈락 논리 기반 의사결정
- ✅ 실제 데이터만 사용
- ✅ M4/M5 전제 조건 명시

---

**ⓒ ZeroSite by AntennaHoldings | Natai Heum**  
**M3 Real Decision Engine Design Document v1.0**
