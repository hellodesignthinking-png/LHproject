# Step7_5 데이터 검증 및 수정 페이지 추가 완료

## 🎯 목표 달성

**요구사항**: 실제 데이터를 사용하기 위한 **데이터 검증 및 수정 페이지** 추가

**핵심 기능**:
- ✅ 토지 기본 정보 (7개 필드) 확인 및 수정
- ✅ 감정평가 정보 (공시지가, 조정률, 최종 단가) 확인 및 수정
- ✅ 거래사례 5-10건 확인 및 추가/삭제/수정
- ✅ POI 정보 (6개 카테고리) 확인 및 수정
- ✅ 모든 데이터 편집 가능
- ✅ 실시간 검증 (최소 5건 거래사례 필수)

---

## 📊 새로운 Step 7.5 구조

### 위치
```
Review (Step 3)
    ↓
📋 Data Verification (Step 3.5) ← NEW!
    ↓
Context Freeze (Step 4)
    ↓
Pipeline (M2-M6)
```

### 4개 섹션 구성

#### Section 1: 🏞️ 토지 기본 정보
```typescript
7개 필드 (모두 수정 가능):
- 주소 (text input)
- 면적 (number input, ㎡ ↔ 평 자동 환산)
- 지목 (select: 대/전/답/임야/잡종지)
- 용도지역 (select: 제1/2/3종일반주거지역, 준주거, 일반상업)
- 용적률 (number input, %)
- 건폐율 (number input, %)
- 도로 폭원 (number input, m)

UI:
- Edit/Save 토글 버튼
- 편집 모드 ON: input 필드 활성화
- 편집 모드 OFF: 읽기 전용 박스 표시
```

#### Section 2: 💰 감정평가 정보
```typescript
3개 필드:
- 기준 공시지가 (number input, 원/㎡)
- 평가 조정률 (number input, %)
- 최종 단가 (자동 계산, 읽기 전용)
  → 계산식: 기준 공시지가 × (1 + 조정률/100)

추가 표시:
- 총 감정평가액 = 최종 단가 × 면적
- 평당 단가 = 최종 단가 × 3.3058

UI:
- Edit/Save 토글 버튼
- 금액 포맷팅 (₩1,500,000 형식)
- 자동 계산 수식 표시
```

#### Section 3: 📊 거래사례 (5-10건)
```typescript
각 사례당 5개 필드:
- 주소 (text input)
- 거래일 (date input)
- 면적 (number input, ㎡)
- 거래금액 (number input, 원)
- 거리 (text input, e.g., "150m")

자동 계산:
- ㎡당 단가 = 거래금액 / 면적

기능:
- ➕ 추가 버튼: 새 거래사례 추가
- 🗑️ 삭제 버튼: 각 사례별 삭제
- Edit/Save 토글: 모든 사례 일괄 편집

검증:
- 최소 5건 필수 (5건 미만 시 경고 표시)
- 제출 버튼 비활성화 (5건 미만 시)

UI:
- 사례별 박스 레이아웃
- 금액 포맷팅
- ㎡당 단가 자동 표시
```

#### Section 4: 🏙️ 주변 시설 (POI)
```typescript
6개 카테고리 (각각 number input):
- 🚇 지하철역 (개)
- 🚌 버스정류장 (개)
- 🏪 편의점 (개)
- 🏥 병원 (개)
- 🏫 학교 (개)
- 🌳 공원 (개)

UI:
- Edit/Save 토글 버튼
- 그리드 레이아웃 (2-3-6 열)
- 아이콘 + 레이블 + 개수
```

---

## 🎯 사용자 경험 (UX)

### 편집 모드 토글
```typescript
각 섹션별 독립적인 Edit/Save 버튼:
- 클릭 시 편집 모드 ON/OFF
- 편집 모드 ON: 모든 필드 input으로 변경
- 편집 모드 OFF: 읽기 전용 박스로 변경
- Save 클릭 시: 자동으로 편집 모드 OFF
```

### 실시간 검증
```typescript
거래사례 최소 5건 검증:
- 현재 건수 표시: "📊 거래사례 (5건)"
- 5건 미만 시:
  - ⚠️ 경고 박스 표시
  - "최소 5건의 거래사례가 필요합니다"
  - 제출 버튼 비활성화 (회색)
- 5건 이상 시:
  - ✅ 검증 완료 박스 표시
  - 제출 버튼 활성화 (파란색)
```

### 포맷팅
```typescript
금액 포맷:
- Input: 1500000
- Display: ₩1,500,000

면적 포맷:
- Input: 500 (㎡)
- Display: "500㎡ (약 151.3평)"

날짜 포맷:
- Input: "2025-12-15"
- Display: "2025-12-15"
```

### 자동 계산
```typescript
1. 최종 단가:
   최종 단가 = 기준 공시지가 × (1 + 조정률/100)
   Example: ₩1,500,000 × (1 + 3.8/100) = ₩1,557,000

2. 총 감정평가액:
   총액 = 최종 단가 × 면적
   Example: ₩1,557,000 × 500㎡ = ₩778,500,000

3. 거래사례 단가:
   단가 = 거래금액 / 면적
   Example: ₩750,000,000 / 480㎡ = ₩1,562,500/㎡
```

---

## 🔧 기술 구현

### 컴포넌트 구조
```typescript
Step7_5DataVerification.tsx
├── Props
│   ├── onComplete: (data: VerificationData) => void
│   ├── onBack: () => void
│   └── initialData?: Partial<VerificationData>
│
├── State
│   ├── isEditing: { [key: string]: boolean }
│   └── data: VerificationData
│       ├── land: LandData (7 fields)
│       ├── appraisal: AppraisalData (3 fields)
│       ├── transactions: TransactionCase[] (5-10 items)
│       └── poi: POIData (6 fields)
│
└── Functions
    ├── toggleEdit(section)
    ├── updateLandData(field, value)
    ├── updateAppraisalData(field, value)
    ├── updatePOIData(field, value)
    ├── updateTransaction(id, field, value)
    ├── addTransaction()
    ├── removeTransaction(id)
    ├── formatCurrency(value)
    └── handleSubmit()
```

### 데이터 플로우
```typescript
1. Initial Load (Step 3 → Step 3.5):
   M1LandingPage passes initialData:
   - land: from formData (selectedAddress, parcelData, cadastralData, etc.)
   - appraisal: defaults
   - transactions: default 5 cases
   - poi: defaults

2. User Editing (Step 3.5):
   - User modifies data
   - Real-time validation
   - Auto-calculation (unit prices, totals)

3. Submission (Step 3.5 → Step 4):
   onComplete(verifiedData) called
   - Data stored in formData.verifiedData
   - Navigate to Step 4 (Context Freeze)

4. Pipeline (Step 4 → M2-M6):
   - formData.verifiedData used for M2 report generation
   - Replaces mock_land_data usage
```

### Integration Points

#### M1LandingPage.tsx 변경사항
```typescript
// Import
import Step7_5DataVerification from './Step7_5DataVerification';

// STEP_LABELS 업데이트
const STEP_LABELS = [
  ...
  '데이터 검토',   // STEP 3: Unified Review
  '데이터 검증',   // STEP 3.5: Data Verification & Edit (NEW!)
  'M1 확정',       // STEP 4: Context Freeze
];

// renderCurrentStep() 추가
case 3:
  return (
    <ReviewScreen
      ...
      onNext={() => goToStep(3.5)} // Changed: was handleReviewComplete
    />
  );

case 3.5: // NEW
  return (
    <Step7_5DataVerification
      initialData={{ ... }}
      onComplete={(verifiedData) => {
        setState(prev => ({
          ...prev,
          formData: {
            ...prev.formData,
            verifiedData: verifiedData
          }
        }));
        goToStep(4);
      }}
      onBack={() => goToStep(3)}
    />
  );

case 4:
  return (
    <Step8ContextFreeze
      formData={state.formData} // Includes verifiedData
      ...
    />
  );
```

---

## 📝 사용 시나리오

### 시나리오 1: 정상 플로우
```
1. Step 3 (Review) 완료
2. Step 3.5 (Data Verification) 진입
   - 기본 데이터 자동 로드 (7개 필드)
   - 5개 거래사례 기본 제공
3. 사용자가 데이터 확인
   - 공시지가 수정: ₩1,500,000 → ₩1,600,000
   - 거래사례 1건 추가 (총 6건)
   - POI 수정: 지하철역 2 → 3
4. "검증 완료 및 다음 단계" 버튼 클릭
5. Step 4 (Context Freeze) 진입
   - verifiedData가 formData에 저장됨
6. 파이프라인 실행
   - M2 보고서에 수정된 데이터 반영
```

### 시나리오 2: 거래사례 부족 (검증 실패)
```
1. Step 3.5 진입
   - 기본 5건 거래사례 로드
2. 사용자가 거래사례 2건 삭제
   - 남은 건수: 3건
3. 경고 메시지 표시:
   "⚠️ 최소 5건의 거래사례가 필요합니다. (현재 3건)"
4. 제출 버튼 비활성화 (회색)
5. 사용자가 거래사례 2건 추가
   - 총 5건
6. 경고 사라지고 제출 버튼 활성화
7. 정상 진행
```

### 시나리오 3: 데이터 대량 수정
```
1. 토지 정보 편집:
   - Edit 버튼 클릭
   - 면적: 500㎡ → 600㎡
   - 용적률: 250% → 280%
   - Save 버튼 클릭
   
2. 감정평가 편집:
   - Edit 버튼 클릭
   - 기준 공시지가: ₩1,500,000 → ₩1,700,000
   - 조정률: 3.8% → 5.0%
   - 최종 단가 자동 계산: ₩1,785,000
   - 총 감정평가액 자동 계산: ₩1,071,000,000
   - Save 버튼 클릭
   
3. 거래사례 편집:
   - Edit 버튼 클릭
   - 사례 3 수정: 거래금액 ₩710,000,000 → ₩750,000,000
   - ㎡당 단가 자동 재계산
   - 사례 6 추가: 새 데이터 입력
   - Save 버튼 클릭
   
4. POI 편집:
   - Edit 버튼 클릭
   - 지하철역: 2 → 3
   - 편의점: 10 → 15
   - Save 버튼 클릭
   
5. 검증 완료 박스 확인:
   ✓ 토지 기본 정보: 600㎡, 제2종일반주거지역
   ✓ 감정평가액: ₩1,071,000,000
   ✓ 거래사례: 6건
   ✓ POI 데이터: 6개 항목 수집 완료
   
6. "검증 완료 및 다음 단계" 클릭
```

---

## 🎨 UI/UX 디자인

### 색상 스킴
```css
Primary (Blue): #3B82F6
Secondary (Purple): #8B5CF6
Success (Green): #10B981
Warning (Yellow): #F59E0B
Error (Red): #EF4444
Gray (Neutral): #6B7280

Gradients:
- Header: from-blue-600 to-purple-600
- Success Box: from-green-50 to-blue-50
- Submit Button: from-blue-600 to-purple-600
```

### 레이아웃
```
┌─────────────────────────────────────┐
│ 📋 데이터 검증 및 수정              │ Header (Gradient)
│ 수집된 데이터를 확인하고 수정하세요 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🏞️ 토지 기본 정보        [Edit 🖊️] │
│ ┌──────────┬──────────┐             │
│ │ 주소     │ 면적     │             │
│ ├──────────┼──────────┤             │
│ │ 지목     │ 용도지역 │             │
│ └──────────┴──────────┘             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 💰 감정평가 정보          [Edit 🖊️] │
│ ┌──────────┬──────────┬──────────┐  │
│ │ 공시지가 │ 조정률   │ 최종단가 │  │
│ └──────────┴──────────┴──────────┘  │
│ 📊 총 감정평가액: ₩778,500,000      │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 📊 거래사례 (5건)    [➕][Edit 🖊️] │
│ ┌─────────────────────────────────┐ │
│ │ 사례 1                   [🗑️]  │ │
│ │ 주소 | 날짜 | 면적 | 금액 | 거리│ │
│ └─────────────────────────────────┘ │
│ (... 4 more cases ...)              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🏙️ 주변 시설 (POI)       [Edit 🖊️] │
│ ┌───┬───┬───┬───┬───┬───┐         │
│ │🚇 │🚌 │🏪 │🏥 │🏫 │🌳 │         │
│ │ 2 │ 5 │ 10│ 2 │ 3 │ 4 │         │
│ └───┴───┴───┴───┴───┴───┘         │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ✅ 데이터 검증 완료                 │
│ ✓ 모든 항목 확인 완료               │
└─────────────────────────────────────┘

[← 이전]              [검증 완료 및 다음 단계 →]
```

---

## 📊 Git 정보

- **최신 커밋**: `0e9487e`
- **브랜치**: `feature/expert-report-generator`
- **PR**: https://github.com/hellodesignthinking-png/LHproject/pull/15
- **변경 파일**:
  - `frontend/src/components/m1/Step7_5DataVerification.tsx` (신규, 775줄)
  - `frontend/src/components/m1/M1LandingPage.tsx` (수정, Step 3.5 추가)

---

## ✅ 완료 체크리스트

- [x] Step7_5DataVerification 컴포넌트 생성
- [x] 4개 섹션 구현 (토지/감정평가/거래사례/POI)
- [x] Edit/Save 토글 기능
- [x] 거래사례 추가/삭제 기능
- [x] 최소 5건 검증 로직
- [x] 자동 계산 (단가, 총액)
- [x] 금액/면적 포맷팅
- [x] M1LandingPage 통합
- [x] STEP_LABELS 업데이트
- [x] Step 3 → 3.5 → 4 플로우 연결
- [x] verifiedData 저장 로직
- [x] Git commit & push

---

## 🔄 다음 단계

### 프론트엔드 테스트
1. 주소 검색 (Step 1-2)
2. 수집 방법 선택 (Step 2.5)
3. 데이터 검토 (Step 3)
4. **데이터 검증 (Step 3.5)** ← NEW!
   - 토지 정보 수정 테스트
   - 감정평가 정보 수정 테스트
   - 거래사례 추가/삭제/수정 테스트
   - POI 수정 테스트
   - 검증 실패 (5건 미만) 테스트
   - 검증 성공 테스트
5. 컨텍스트 Freeze (Step 4)
6. 파이프라인 실행
7. **M2 보고서에서 실제 데이터 확인** ← 중요!

### 백엔드 연동 (다음 작업)
```python
# pipeline_reports_v4.py 수정 필요
async def run_pipeline_analysis(request: PipelineAnalysisRequest):
    # 기존: mock_land_data 사용
    mock_land_data = request.mock_land_data
    
    # 신규: verifiedData 사용 (우선순위 높음)
    if request.verified_data:
        land_data = request.verified_data.land
        appraisal_data = request.verified_data.appraisal
        transactions = request.verified_data.transactions
        poi_data = request.verified_data.poi
    else:
        land_data = request.mock_land_data
        # fallback to mock data
```

---

**상태**: 프론트엔드 Step7_5 구현 완료  
**다음 작업**: Step7_5에서 입력한 데이터를 백엔드 파이프라인에 전달하여 M2 보고서에 반영
