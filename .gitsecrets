# ============================================
# Git-Secrets Configuration for ZeroSite
# ============================================
#
# This file contains patterns to prevent committing sensitive data
# 
# Installation:
#   brew install git-secrets  # macOS
#   apt-get install git-secrets  # Ubuntu
#
# Setup (run in project root):
#   git secrets --install
#   git secrets --register-aws
#   git secrets --add-provider -- cat .gitsecrets
#
# ============================================

# API Key Patterns
[A-Za-z0-9]{32,}

# Common API Key Formats
(api[_-]?key|apikey)['\"]?\s*[:=]\s*['\"]?[A-Za-z0-9]{20,}
(secret[_-]?key|secretkey)['\"]?\s*[:=]\s*['\"]?[A-Za-z0-9]{20,}
(access[_-]?key|accesskey)['\"]?\s*[:=]\s*['\"]?[A-Za-z0-9]{20,}

# Kakao API Keys
(kakao[_-]?api[_-]?key)['\"]?\s*[:=]\s*['\"]?[A-Za-z0-9]{20,}

# Google API Keys
AIza[0-9A-Za-z\\-_]{35}

# AWS Access Keys
AKIA[0-9A-Z]{16}

# Private Keys
-----BEGIN\s+(RSA\s+)?PRIVATE\s+KEY-----

# OAuth Tokens
[0-9a-fA-F]{40}

# JWT Tokens
eyJ[A-Za-z0-9_-]*\.eyJ[A-Za-z0-9_-]*\.[A-Za-z0-9_-]*

# Database URLs with passwords
(postgres|mysql|mongodb):\/\/[^:]+:[^@]+@

# Generic Secrets
(password|passwd|pwd)['\"]?\s*[:=]\s*['\"]?[^\s'\"]{8,}
