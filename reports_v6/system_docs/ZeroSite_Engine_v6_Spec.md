# ZeroSite Land Engine v6.0 기술 사양서
## Technical Specification Document

**버전:** v6.0 (2025 Q1 Release Candidate)  
**작성일:** 2025-12-01  
**작성자:** ZeroSite R&D Team  
**분류:** 사내 기술문서 (Confidential)

---

## 📋 문서 개요 (Document Overview)

### 목적 (Purpose)
본 문서는 ZeroSite Land Analysis Engine v6.0의 기술적 사양, 아키텍처, 모듈 구성, API 인터페이스를 정의합니다. v5.1의 성공적인 배포 결과를 바탕으로, v6.0은 다음을 목표로 합니다:

1. **성능 개선**: 분석 시간 6분 → 3분 (50% 단축)
2. **정확도 향상**: LH 승인율 82.3% → 88.0% (+5.7%p)
3. **신규 기능 추가**: Geo Optimizer v2.0, Multi-Parcel Engine v2.0
4. **확장성 강화**: Microservices 아키텍처 전환

### 주요 변경사항 (Key Changes from v5.1)

| 항목 | v5.1 | v6.0 | 개선율 |
|------|------|------|--------|
| **분석 시간** | 6분 | 3분 | 50% ↓ |
| **LH 승인율** | 82.3% | 88.0% | +5.7%p |
| **API 응답시간** | 2.5초 | 1.2초 | 52% ↓ |
| **동시처리** | 10건 | 50건 | 400% ↑ |
| **메모리 사용량** | 4GB | 2.5GB | 37.5% ↓ |

---

## 🏗️ 시스템 아키텍처 (System Architecture)

### 1. 전체 구조 (Overall Architecture)

```
┌─────────────────────────────────────────────────────────────┐
│                     Frontend Layer                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  Web UI     │  │  Mobile App │  │  CLI Tool   │       │
│  │  (React)    │  │  (Flutter)  │  │  (Python)   │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────────────────────────┘
                             ↓ HTTPS/REST API
┌─────────────────────────────────────────────────────────────┐
│                     API Gateway                             │
│           (Kong / AWS API Gateway)                          │
│     ┌───────────────┬───────────────┬───────────────┐     │
│     │ Auth Service  │ Rate Limiter  │ Load Balancer │     │
│     └───────────────┴───────────────┴───────────────┘     │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                 Microservices Layer                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Analysis     │  │ Geo          │  │ ESG          │    │
│  │ Orchestrator │  │ Optimizer    │  │ Scoring      │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Multi-Parcel │  │ AI Auto      │  │ LH Scorecard │    │
│  │ Engine       │  │ Corrector    │  │ Engine       │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                   Data Layer                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ PostgreSQL   │  │ Redis Cache  │  │ S3 Storage   │    │
│  │ (Primary DB) │  │ (Session)    │  │ (Reports)    │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│              External API Integration Layer                 │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │Kakao │ │VWorld│ │건축물│ │토지이│ │기상청│ │KOSIS │  │
│  │ Map  │ │ (국토│ │대장  │ │용규제│ │OpenM │ │(통계)│  │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2. 분석 프로세스 흐름 (Analysis Process Flow)

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Input Validation & Geocoding                        │
│  → 주소 정규화 → 좌표 변환 → 행정구역 매핑                     │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Parallel Module Execution (병렬 처리)                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Traffic      │  │ Demand       │  │ Regulation   │     │
│  │ Analyzer     │  │ Density      │  │ Checker      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Architecture │  │ ESG Scoring  │  │ Finance      │     │
│  │ Analysis     │  │ Engine       │  │ Calculator   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: AI Auto Corrector (자동 보정)                        │
│  → 이상값 탐지 → 정책 변경 반영 → 데이터 정합성 검증           │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: LH Scorecard Calculation                            │
│  → 350점 만점 평가 → 가점/감점 산정 → 합격 가능성 예측        │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 5: Geo Optimizer (Optional)                            │
│  → 5km 반경 대안지 탐색 → 최적 필지 추천                      │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 6: Report Generation                                   │
│  → Markdown → HTML → PDF → Dashboard                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 핵심 모듈 사양 (Core Module Specifications)

### Module 1: Traffic Analyzer (교통 접근성 분석)

**기능:** 대중교통, 도로 접근성 분석 및 LH 평가 점수 산출

**입력 (Input):**
```json
{
  "latitude": 37.5665,
  "longitude": 126.9780,
  "address": "서울시 중구 세종대로 110"
}
```

**처리 과정 (Processing):**
1. **지하철/경전철 거리 계산**
   - Haversine 공식 적용
   - Gaussian Decay 모델: `score = 25 * exp(-(distance/500)^2)`
   - GTX/광역철도 가점: +3점
   - 환승역 가점: +2점

2. **버스 정류장 분석**
   - 반경 300m 내 정류장 수
   - 노선 수 및 배차 간격

3. **도로 접근성**
   - 고속도로 IC 거리
   - 주요 간선도로 접근성

**출력 (Output):**
```json
{
  "traffic_score": 22,
  "max_score": 25,
  "details": {
    "metro_distance": 280,
    "metro_name": "서울역",
    "bus_stops": 8,
    "highway_ic": "남산1호터널",
    "highway_distance": 1200
  },
  "bonuses": [
    {"type": "GTX", "points": 3},
    {"type": "Transfer Station", "points": 2}
  ]
}
```

**성능 목표:**
- 응답 시간: < 0.5초
- 정확도: 95% (실측 거리 대비)

---

### Module 2: ESG Scoring Engine v2.0

**기능:** ZEB, G-SEED, K-ESG 평가 및 LH 인센티브 자동 계산

**입력 (Input):**
```json
{
  "building_area": 3500,
  "floors": 10,
  "heating_type": "개별난방",
  "insulation": "0.15 W/m²K",
  "solar_capacity": 120,
  "green_ratio": 0.25
}
```

**처리 과정 (Processing):**

1. **ZEB 등급 산정**
```python
# 에너지자립률 계산
energy_self_sufficiency = (solar_capacity * 1000 * 365 * 3.5) / (building_area * 150)

# ZEB 등급 판정
if energy_self_sufficiency >= 100: grade = "ZEB 1등급"
elif energy_self_sufficiency >= 80: grade = "ZEB 2등급"
elif energy_self_sufficiency >= 60: grade = "ZEB 3등급"
elif energy_self_sufficiency >= 40: grade = "ZEB 4등급"
else: grade = "ZEB 5등급"

# LH 가점 매핑
lh_points = {
    "ZEB 1등급": 15,
    "ZEB 2등급": 12,
    "ZEB 3등급": 9,
    "ZEB 4등급": 6,
    "ZEB 5등급": 3
}
```

2. **G-SEED 점수 계산**
   - 8개 카테고리 평가 (토지, 교통, 에너지, 재료, 수자원, 유지관리, 생태, 실내환경)
   - 가중치 적용: 에너지 (32%), 재료/자원 (24%), 실내환경 (18%)

3. **K-ESG 로드맵 제시**
   - 환경(E) 40점: ZEB, 탄소배출, 폐기물
   - 사회(S) 40점: 지역사회 기여, 안전보건
   - 지배구조(G) 20점: 윤리경영, 투명성

**출력 (Output):**
```json
{
  "zeb": {
    "grade": "ZEB 3등급",
    "energy_self_sufficiency": 23.8,
    "lh_points": 9,
    "annual_savings": 12500000
  },
  "gseed": {
    "total_score": 82,
    "grade": "우수등급",
    "lh_points": 6,
    "categories": {
      "land": 16, "transport": 10, "energy": 28, "materials": 18,
      "water": 12, "maintenance": 8, "ecology": 6, "indoor": 14
    }
  },
  "kesg": {
    "total_score": 72,
    "grade": "B",
    "e_score": 30, "s_score": 28, "g_score": 14
  },
  "total_lh_points": 15
}
```

---

### Module 3: Geo Optimizer v2.0

**신규 기능:**
- **3km 반경 환경 분석**: 일조, 바람길, 소음
- **AI 기반 필지 추천**: 머신러닝 모델 (Random Forest)
- **다필지 통합 시뮬레이션**: 2-5개 필지 조합 최적화

**입력 (Input):**
```json
{
  "center_lat": 37.5665,
  "center_lng": 126.9780,
  "search_radius": 5000,
  "constraints": {
    "min_area": 600,
    "max_price_per_sqm": 5000000,
    "zoning": ["제2종일반주거지역", "준주거지역"]
  }
}
```

**처리 과정 (Processing):**

1. **필지 후보 탐색**
   - VWorld API: 지적도 기반 필지 추출
   - 필터링: 면적, 용도지역, 소유권

2. **다변량 점수화**
```python
# 최적화 점수 = 가중합
score = (
    0.25 * traffic_accessibility_score +
    0.20 * lh_evaluation_score +
    0.15 * esign_incentive_score +
    0.15 * land_price_score +
    0.10 * future_value_score +
    0.10 * regulatory_risk_score +
    0.05 * construction_ease_score
)
```

3. **환경 분석 (신규)**
   - **일조권 분석**: 동지 기준 4시간 확보 여부
   - **바람길 분석**: Open-METEO API + 지형 데이터
   - **소음 분석**: 도로 교통량, 철도, 공항 거리

**출력 (Output):**
```json
{
  "alternatives": [
    {
      "rank": 1,
      "address": "서울 마포구 합정동 123-45",
      "parcel_id": "1144010100100450000",
      "area": 850,
      "price_per_sqm": 4200000,
      "lh_score": 312,
      "roi": 8.5,
      "optimization_score": 92.5,
      "environment": {
        "sunlight_hours": 5.2,
        "wind_direction": "남서풍 (여름)",
        "noise_level": 52
      }
    }
  ],
  "recommendation": "현위치 최적 - 대안지 변경 불필요"
}
```

---

### Module 4: Multi-Parcel Engine v2.0

**기능:** 2-5개 필지 통합 개발 시뮬레이션

**입력 (Input):**
```json
{
  "parcels": [
    {"id": "1144010100100450001", "area": 500},
    {"id": "1144010100100450002", "area": 650}
  ],
  "development_type": "통합개발"
}
```

**처리 과정 (Processing):**

1. **합필/분할 시뮬레이션**
   - 합필 가능성 검토 (소유권, 도로 접면, 고저차)
   - 분할 후 최소 면적 충족 여부

2. **LH 가점 계산**
   - 다필지 통합 가점: +5점 (2-3개), +8점 (4-5개)
   - 정비사업 구역 내: 추가 +3점

3. **사업비 증감 분석**
   - 합필 비용: 감정평가, 등기, 측량
   - 공사비 절감: 공사 단위 규모화

**출력 (Output):**
```json
{
  "merged_parcel": {
    "total_area": 1150,
    "lh_bonus_points": 5,
    "roi_improvement": 1.2,
    "additional_costs": 35000000,
    "construction_savings": 82000000,
    "net_benefit": 47000000
  },
  "recommendation": "합필 추진 권장 (ROI +1.2%p)"
}
```

---

### Module 5: AI Auto Corrector (Enhanced)

**기능:** 실시간 정책 반영 및 데이터 이상값 자동 보정

**v6.0 신규 기능:**
- **LH 정책 변경 자동 추적**: GitHub Actions + Web Scraping
- **머신러닝 기반 이상값 탐지**: Isolation Forest 알고리즘
- **자동 Fallback 메커니즘**: API 장애 시 캐시 데이터 활용

**정책 추적 프로세스:**
```python
# 1. LH 공고문 자동 스크래핑 (매일 02:00 KST)
def scrape_lh_notices():
    url = "https://www.lh.or.kr/notice/landlease"
    notices = parse_notices(url)
    
    # 변경사항 감지
    for notice in notices:
        if is_policy_change(notice):
            update_policy_db(notice)
            send_alert_to_admins(notice)

# 2. 평가 기준 자동 업데이트
def update_scoring_rules(policy_change):
    # 예시: 건설비 연동 90% -> 110%
    if "건설비 연동" in policy_change.title:
        update_parameter("construction_cost_multiplier", 1.10)
        
    # 예시: ESG 배점 확대 30점 -> 45점
    if "ESG 평가" in policy_change.title:
        update_parameter("esg_max_score", 45)
```

**이상값 탐지 예시:**
```python
from sklearn.ensemble import IsolationForest

# 학습 데이터: 과거 1000건 분석 결과
X_train = load_historical_data(["traffic_score", "roi", "lh_score"])

# Isolation Forest 모델 훈련
model = IsolationForest(contamination=0.05)
model.fit(X_train)

# 신규 데이터 이상값 탐지
new_data = [[22, 8.5, 312]]
prediction = model.predict(new_data)

if prediction == -1:  # 이상값
    alert = "⚠️ 비정상적으로 높은 ROI 감지 - 검증 필요"
    log_warning(alert)
```

---

### Module 6: LH Scorecard Engine

**기능:** LH 신축매입임대 평가 350점 자동 산출

**평가 항목 (12개 카테고리):**

| 항목 | 배점 | 평가 방법 |
|------|------|-----------|
| ① 교통 접근성 | 25 | Gaussian 거리감소 모델 |
| ② 생활편의시설 | 20 | 5개 시설 거리 점수화 |
| ③ 수요 밀도 | 15 | 인구통계 + 가구 특성 |
| ④ 사업계획 적정성 | 30 | 설계, 일정, 수익성 |
| ⑤ 시공사 신용도 | 20 | 신용등급 + 실적 |
| ⑥ 재무건전성 | 25 | 자기자본비율, PF 구조 |
| ⑦ ESG 평가 | 45 | ZEB, G-SEED, K-ESG |
| ⑧ 유형별 특화 | 15 | 청년/신혼/고령자 맞춤 |
| ⑨ 지역 균형발전 | 20 | 낙후지역, 개발 필요성 |
| ⑩ 혁신기술 | 10 | IoT, 스마트홈, AI |
| ⑪ 주거복지 연계 | 10 | 복지시설, 지역사회 |
| ⑫ 정책 부합도 | 15 | 정부 정책 반영 |
| **비수도권 가점** | +8 | 지역별 차등 |
| **다필지 통합** | +5 | 2개 이상 필지 |
| **합계** | **350** | - |

**알고리즘 상세:**
```python
def calculate_lh_score(data):
    score = 0
    
    # ① 교통 접근성 (25점)
    metro_dist = data["metro_distance"]
    traffic_score = 25 * math.exp(-(metro_dist / 500) ** 2)
    if data["gtx_station"]: traffic_score += 3
    if data["transfer_station"]: traffic_score += 2
    score += min(traffic_score, 25)
    
    # ② 생활편의시설 (20점)
    facilities = {
        "mart": (500, 5),
        "hospital": (1000, 5),
        "park": (300, 4),
        "bank": (300, 3),
        "cultural": (1000, 3)
    }
    for facility, (max_dist, max_score) in facilities.items():
        dist = data[f"{facility}_distance"]
        score += max_score * max(0, 1 - dist / max_dist)
    
    # ③ 수요 밀도 (15점)
    if data["target_population_ratio"] >= 0.25:
        score += 5
    if data["single_household_ratio"] >= 0.30:
        score += 4
    if data["newlywed_ratio"] >= 0.15:
        score += 3
    # ... (continue for all 12 categories)
    
    # 가점 계산
    if data["region"] != "수도권":
        score += 8
    if len(data["parcels"]) >= 2:
        score += 5
    
    return min(score, 350)
```

---

### Module 7: Finance Calculator

**기능:** ROI, IRR, NPV 자동 계산 및 민감도 분석

**입력 (Input):**
```json
{
  "land_cost": 6600000000,
  "construction_cost": 8415000000,
  "equity_ratio": 0.30,
  "pf_interest_rate": 0.042,
  "project_duration": 24
}
```

**계산 프로세스:**

1. **LH 매입가 추정**
```python
# 건설비 110% 연동제 (2025년 기준)
lh_purchase_price = (
    land_cost +
    construction_cost * 1.10 +
    other_costs * 0.80
)
```

2. **ROI 계산**
```python
total_project_cost = land_cost + construction_cost + other_costs
profit = lh_purchase_price - total_project_cost
roi = (profit / total_project_cost) * 100
```

3. **IRR 계산** (numpy_financial 활용)
```python
import numpy_financial as npf

cash_flows = [
    -equity_investment,  # Month 0
    -construction_payments,  # Month 1-18
    lh_purchase_price - pf_loan_repayment  # Month 24
]

irr = npf.irr(cash_flows) * 12  # 연간 IRR
```

4. **NPV 계산** (WACC 기반)
```python
# WACC 계산
wacc = (
    equity_ratio * cost_of_equity +
    (1 - equity_ratio) * pf_interest_rate * (1 - corporate_tax_rate)
)

npv = npf.npv(wacc / 12, cash_flows)
```

5. **민감도 분석**
```python
# 5개 변수 ±10% 변동
sensitivity_vars = [
    "lh_purchase_price",
    "construction_cost",
    "pf_interest_rate",
    "land_cost",
    "project_duration"
]

for var in sensitivity_vars:
    for delta in [-0.10, -0.05, 0, 0.05, 0.10]:
        modified_value = base_value * (1 + delta)
        new_roi = recalculate_roi(var, modified_value)
        sensitivity_results[var][delta] = new_roi
```

**출력 (Output):**
```json
{
  "base_scenario": {
    "roi": 8.0,
    "irr": 24.5,
    "npv": 1250000000
  },
  "worst_case": {
    "roi": 3.2,
    "irr": 10.5,
    "npv": -350000000
  },
  "best_case": {
    "roi": 12.8,
    "irr": 38.2,
    "npv": 3200000000
  },
  "sensitivity": {
    "lh_purchase_price": {"impact": "High", "elasticity": 0.85},
    "construction_cost": {"impact": "Low", "elasticity": 0.12},
    "pf_interest_rate": {"impact": "Medium", "elasticity": -0.45}
  }
}
```

---

## 🔌 API 명세 (API Specifications)

### REST API Endpoints

#### 1. 분석 요청 (POST /api/v6/analyze)

**Request:**
```json
{
  "address": "서울시 마포구 합정동 123-45",
  "area": 800,
  "type": "청년형",
  "options": {
    "enable_geo_optimizer": true,
    "enable_multi_parcel": false,
    "generate_report": true
  }
}
```

**Response (202 Accepted):**
```json
{
  "job_id": "zs-20251201-abc123",
  "status": "processing",
  "estimated_time": 180,
  "polling_url": "/api/v6/jobs/zs-20251201-abc123"
}
```

#### 2. 작업 상태 조회 (GET /api/v6/jobs/{job_id})

**Response (200 OK):**
```json
{
  "job_id": "zs-20251201-abc123",
  "status": "completed",
  "progress": 100,
  "result": {
    "lh_score": 312,
    "roi": 8.5,
    "irr": 26.2,
    "esg_total_points": 18,
    "recommendation": "강력 추천 (A+)"
  },
  "report_url": "https://api.zerosite.ai/reports/zs-20251201-abc123.pdf"
}
```

#### 3. Geo Optimizer (POST /api/v6/geo-optimizer)

**Request:**
```json
{
  "center_lat": 37.5665,
  "center_lng": 126.9780,
  "radius": 5000,
  "constraints": {
    "min_area": 600,
    "max_price": 5000000000
  }
}
```

**Response (200 OK):**
```json
{
  "alternatives": [
    {
      "rank": 1,
      "address": "서울 마포구 합정동 456-78",
      "lh_score": 318,
      "roi": 8.8,
      "optimization_score": 94.2
    }
  ]
}
```

---

## ⚡ 성능 최적화 (Performance Optimization)

### 1. 병렬 처리 (Parallel Processing)
- **Celery + Redis**: 비동기 작업 큐
- **Multiprocessing**: CPU-bound 작업 병렬화
- **목표**: 7개 모듈 동시 실행 → 분석 시간 6분 → 3분

### 2. 캐싱 전략 (Caching Strategy)
```python
# Redis 캐싱 레이어
CACHE_TTL = {
    "address_geocoding": 86400,  # 24시간
    "lh_policy": 3600,  # 1시간
    "traffic_data": 1800,  # 30분
    "land_price": 21600  # 6시간
}
```

### 3. 데이터베이스 최적화
- PostgreSQL 인덱싱: 주소, 좌표, 날짜
- Connection Pooling: pgbouncer
- Read Replica: 읽기 부하 분산

### 4. API 호출 최적화
- **Batch Requests**: 10개 주소 동시 조회
- **Rate Limiting**: 429 Error 방지
- **Circuit Breaker**: 외부 API 장애 대응

---

## 🔒 보안 및 인증 (Security & Authentication)

### 1. API 인증
```python
# JWT 기반 인증
headers = {
    "Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "Content-Type": "application/json"
}
```

### 2. 데이터 암호화
- **전송 중**: TLS 1.3
- **저장 시**: AES-256 (민감 정보)

### 3. Rate Limiting
- **Free Tier**: 10 requests/hour
- **Pro Tier**: 100 requests/hour
- **Enterprise**: Unlimited

---

## 📊 모니터링 및 로깅 (Monitoring & Logging)

### 1. 메트릭 수집
- Prometheus + Grafana
- 주요 메트릭:
  - API 응답 시간 (p50, p95, p99)
  - 에러율
  - 동시 사용자 수
  - 외부 API 호출 성공률

### 2. 로깅 구조
```json
{
  "timestamp": "2025-12-01T14:30:00Z",
  "level": "INFO",
  "service": "traffic-analyzer",
  "job_id": "zs-20251201-abc123",
  "duration_ms": 452,
  "result": {"score": 22}
}
```

---

## 🚀 배포 전략 (Deployment Strategy)

### 1. CI/CD 파이프라인
```yaml
# .github/workflows/deploy.yml
name: ZeroSite v6.0 Deployment
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        run: pytest tests/
      - name: Build Docker Image
        run: docker build -t zerosite:v6.0 .
      - name: Deploy to ECS
        run: aws ecs update-service --service zerosite-prod
```

### 2. Blue-Green Deployment
- 무중단 배포
- 트래픽 점진적 전환 (10% → 50% → 100%)

---

## 📚 버전 히스토리 (Version History)

| 버전 | 출시일 | 주요 변경사항 |
|------|--------|---------------|
| **v6.0** | 2025-03 (예정) | Geo Optimizer v2.0, Multi-Parcel Engine v2.0, 성능 50% 개선 |
| v5.1 | 2025-12 | 45.5페이지 보고서, ESG 확장, 30+ 배제기준 |
| v5.0 | 2024-11 | LH Scorecard 350점, AI Auto Corrector |
| v4.2 | 2024-08 | 초기 프로덕션 릴리스 |

---

**문서 버전:** 1.0  
**승인자:** CTO  
**다음 리뷰 예정:** 2025-01-15

**© 2025 ZeroSite. All Rights Reserved.**
